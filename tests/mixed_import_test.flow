-- Test mixed selective imports (some with aliases, some without)
circle {add, subtract as sub, multiply, PI as pi} from "modules/calc.flow"

shout("Testing mixed imports:")
shout("add(5, 3) = " + add(5, 3))
shout("sub(10, 4) = " + sub(10, 4))
shout("multiply(6, 7) = " + multiply(6, 7))
shout("pi = " + pi)

shout("✅ Mixed import test passed!")
